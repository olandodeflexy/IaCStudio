// PolicyStudio panel — browse Rego/Sentinel policies, run them against
// the active project, and view findings. Wired in the "new-feature
// panels" commit once the backend endpoints are confirmed stable.
export function PolicyStudioPanel() {
  return null;
}
