// ModuleRegistry panel — search the module registry + promote a canvas
// selection to a reusable module. Wired in the later commit once the
// selection API is stable.
export function ModuleRegistryPanel() {
  return null;
}
