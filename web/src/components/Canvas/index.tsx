// Canvas panel — owns the React Flow graph + swimlane layout engine.
// The real implementation lands in the "swimlane canvas" commit; this
// stub exists so routing and the App.tsx split can reference a stable
// import path.
export function CanvasPanel() {
  return null;
}
